{
  "name": "heartcraft",
  "version": "0.0.1",
  "description": "Install AI personality prompts (Hearts) from HeartCraftLab into your Claude Code skills.",
  "type": "module",
  "main": "dist/cli.js",
  "bin": {
    "heartcraft": "dist/cli.js"
  },
  "files": [
    "dist",
    "README.md",
    "LICENSE"
  ],
  "scripts": {
    "build": "tsc",
    "dev": "tsx src/cli.ts",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "commander": "^14.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  }
}
